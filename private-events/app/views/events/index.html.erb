<h1>Events</h1>

<% if @events.empty? %>
  <p>No events yet.</p>
<% else %>
  <ul>
    <% @events.each do |event| %>
      <li><%= link_to event.name, event_path(event) %></li>
    <% end %>
  </ul>
<% end %>